<!doctype html>
<html lang="en">
  <head>
    {{> head}}
  </head>
  <body class="tb-body">
    {{> header}}

    <div class="tb-shell">
      <aside class="tb-sidebar" aria-label="Site navigation">
        {{> nav}}
      </aside>

      <main class="tb-main">
        <article class="doc">
          <h1>404</h1>
          <p>That page doesn’t exist.</p>
          <p><a href="{{siteRootPath}}">Back to home</a></p>
        </article>
      </main>
    </div>

    {{> footer}}
    <script src="{{uiRootPath}}/js/site.js"></script>
  </body>
</html>

